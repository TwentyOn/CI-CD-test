pipeline {
    agent {
        docker {
            image 'python:3.14-alpine'
        }
    }

    stages {
        stage('Build') {
            steps {
                sh 'ls -a'
            }
        }
    }
}